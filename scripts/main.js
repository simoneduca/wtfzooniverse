
$(function() {

    // Populate using an object literal
    // WTF.init({ your: Object });

    // Populate using a JSON file
    WTF.init( 'zooniverse.json' );

    // Populate using a Google spreadsheet ID (you must publish it first!)
    // WTF.init( 'yourGoogleSpreadsheetID' );

});
